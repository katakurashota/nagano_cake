<div class ="container">
 <div class ="row">
  <h5>ショッピングカート</h5>
 </div>
 <% @cart_items.each do |cart_item| %>
 <table class="table">
 <div class="row">
   <tr>
     <th>商品名</th>
     <th>単価（税込</th>
     <th>数量</th>
     <th>小計</th>
     <th></th>
   </tr>
   <tr>
     <td><%= cart_item.item.name %></td>
     <td><%= (cart_item.item.price*1.08).to_i %></td>
   </tr>
   </div>
   </table>
   <% end %>
</div>